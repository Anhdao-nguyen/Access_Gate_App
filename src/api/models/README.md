# Models
This directory defines the **Shape of your Data**.

- **What goes here?**: Definitions of what a "Guest", "User", or "Log" looks like in the database.
- **Example**: `GuestModel.js` might define that a guest has a `name`, `id_number`, and `check_in_time`.

If you have a bug where data is missing or saved in the wrong format, look here.
