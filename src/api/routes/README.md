# Routes
This directory acts as the **Traffic Controller**.

- **What goes here?**: Files that decide which URL (e.g., `/api/login`) goes to which Controller.
- **Example**: `authRoutes.js` says "When user goes to /login, run `AuthController.login`".

If the app says "404 Not Found" or the button calls the wrong function, look here.
